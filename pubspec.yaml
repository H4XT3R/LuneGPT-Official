name: lunegpt_app
description: Elite Intelligence optimized by Adam Aghnia.
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # Using 'any' allows the solver to bypass the "no versions match" error
  llama_flutter_android: any 
  path_provider: any
  cupertino_icons: any

flutter:
  uses-material-design: true
