name: Build LuneGPT
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Regenerate and Fix SDK
        run: |
          rm -rf android
          flutter create . --platforms android --project-name lunegpt
          
          # This command now targets the .kts file and uses Kotlin syntax
          sed -i 's/minSdk = .*/minSdk = 26/' android/app/build.gradle.kts
          
      - name: Install NDK
        run: echo "y" | sudo ${ANDROID_SDK_ROOT}/cmdline-tools/latest/bin/sdkmanager --install "ndk;27.0.12077973"

      - name: Build APK
        run: flutter build apk --release --no-shake-icons

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: LuneGPT-Release
          path: build/app/outputs/flutter-apk/app-release.apk
